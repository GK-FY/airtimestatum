# Server
PORT=3000
BASE_URL=http://localhost:3000

# WhatsApp admin alert number (international without +), e.g. 2547XXXXXXXX
ADMIN_WHATSAPP=254110307506

# Admin UI token — change to a strong secret before deploying
ADMIN_UI_TOKEN=changeme-strong-token

# Where LocalAuth session is stored
SESSION_DIR=./session

# Default behavior: how many seconds to poll Shadow for payment status after STK
POLL_SECONDS=20
POLL_INTERVAL=5
